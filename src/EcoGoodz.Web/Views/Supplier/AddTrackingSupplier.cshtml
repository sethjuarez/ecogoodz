@model EcoGoodz.Web.Models.Supplier.SupplierTrackingSupplierFormViewModel
@{
    ViewData["Title"] = "Add tracking supplier";
}

<div class="page-header d-print-none mb-3">
    <div class="row g-2 align-items-center">
        <div class="col">
            <div class="page-pretitle">Supplier tracking</div>
            <h2 class="page-title">Add supplier</h2>
            <div class="text-secondary">@Model.ProductName</div>
        </div>
        <div class="col-auto ms-auto">
            <a asp-action="Tracking" asp-route-userId="@Model.CurrentUserId" class="btn">Back to tracking</a>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="AddTrackingSupplier" method="post">
            <input asp-for="CurrentUserId" type="hidden" />
            <input asp-for="UserProductId" type="hidden" />
            <input asp-for="ProductId" type="hidden" />
            <input asp-for="ProductName" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            <div class="mb-3">
                <label asp-for="SupplierId" class="form-label"></label>
                <select asp-for="SupplierId" asp-items="Model.SupplierOptions" class="form-select" data-searchable-select="true" data-search-url="@Url.Action("SearchTrackingSuppliers", "Supplier", new { productId = Model.ProductId })">
                    <option value="">Choose a supplier</option>
                </select>
                <span asp-validation-for="SupplierId" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="LocationId" class="form-label"></label>
                <select asp-for="LocationId" asp-items="Model.LocationOptions" class="form-select">
                    <option value="">Choose a location</option>
                </select>
                <span asp-validation-for="LocationId" class="text-danger"></span>
                <div class="form-hint">Locations are limited to active supplier-product locations for @Model.ProductName.</div>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Save supplier</button>
                <a asp-action="Tracking" asp-route-userId="@Model.CurrentUserId" class="btn">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        (() => {
            const supplier = document.getElementById('SupplierId');
            const location = document.getElementById('LocationId');
            const productId = document.getElementById('ProductId')?.value;
            const url = '@Url.Action("GetTrackingSupplierLocations", "Supplier")';

            async function loadLocations() {
                const supplierId = supplier.value;
                location.innerHTML = '<option value="">Choose a location</option>';
                if (!supplierId || !productId) {
                    return;
                }

                const response = await fetch(`${url}?productId=${encodeURIComponent(productId)}&supplierId=${encodeURIComponent(supplierId)}`);
                if (!response.ok) {
                    return;
                }

                const items = await response.json();
                for (const item of items) {
                    const option = document.createElement('option');
                    option.value = item.id;
                    option.textContent = item.text;
                    location.appendChild(option);
                }
            }

            supplier?.addEventListener('change', loadLocations);
        })();
    </script>
}
