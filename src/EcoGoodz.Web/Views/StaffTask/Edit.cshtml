@model EcoGoodz.Web.Models.StaffTask.StaffTaskFormViewModel
@{
    ViewData["Title"] = "Edit task";
}

<div class="page-header d-print-none mb-3">
    <div class="row g-2 align-items-center">
        <div class="col">
            <div class="page-pretitle">Tasks</div>
            <h2 class="page-title">Edit task</h2>
        </div>
    </div>
</div>

<form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="card">
    @await Html.PartialAsync("_Form", Model)
    <div class="card-footer d-flex justify-content-between">
        <button type="submit" class="btn btn-primary">Save changes</button>
        <a asp-action="Index" class="btn">Cancel</a>
    </div>
</form>

<form asp-action="Deactivate" asp-route-id="@Model.Id" method="post" class="mt-3 text-end">
    <button type="submit" class="btn btn-outline-danger">Deactivate task</button>
</form>
